package midi.messages

import midi.messages.NoteOn
import org.scalatest.FunSpec

class NoteOnSpec extends FunSpec {
  describe("getBytes") {
    it("should output a byteString that matches the specification") {

    }
  }
}
